package generate

const (
	controls      = "controls"
	contextDir    = "svc"
	wireDir       = "wire"
	groupProperty = "group"
	types         = "types"

	middlewareDir = controls + "/middleware"
	typesDir      = controls + "/types"
)
